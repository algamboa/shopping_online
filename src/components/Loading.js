import imgLoading from './../images/switch800.gif';
const Loading = () => {
    return (
        <>
            <img alt="Loading" src={imgLoading} style={{ maxWidth: "100%"}} />
        </>
    )
}

export default Loading;