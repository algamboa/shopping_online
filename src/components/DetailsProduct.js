import { Col, Card, Button } from 'react-bootstrap';

const DetailsProduct = ({ key, image, title, text, price }) => {
    return (
        <Col key={key}>
            <h1>Hola Mundo</h1>
        </Col>
    );
}
export default DetailsProduct;